
<p style="color: green"><%= notice %></p>

<h1>Registries</h1>


<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Location:</th>
      <th>State:</th>
      <th></th>
    </tr>


  </thead>
   <tbody>
      <% @registries.each do |registry| %>
    <tr>
      <td><%= registry.name %></td>
      <td><%= registry.location %></td>
      <td><%= registry.state %></td>
      <td><%= link_to "view", registry %></td>
    </tr>
      <% end %>

  </tbody>

</table>

<%= link_to "New registry", new_registry_path %>
